import { Header } from "../components/header";
import { MainInfo } from "../components/mainInfo";

export const HomePage = () => {
  return (
    <div>
      <Header />
      <MainInfo />
    </div>
  );
};
